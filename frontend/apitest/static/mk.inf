SUBDIRS = bootstrap334 css imgs js

LINKS = *.js

all::

clean::

install::
	${MKDIR} -p $(TOPDIR)/OCTFrame/static/
	${COPY} *.js $(TOPDIR)/OCTFrame/static/.

real_install::
	${MKDIR} -p $(DSTDIR)/OCTFrame/static/.
	${COPY} *.js $(DSTDIR)/OCTFrame/static/.
